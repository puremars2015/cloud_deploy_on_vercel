# Neon Postgres 連線字串。
# 在 Vercel 用 Marketplace 接上 Neon 後，這個變數會自動注入到專案，不用手動設定。
# 本機開發時，從 Neon Dashboard 或 `vercel env pull` 取得後填在 .env（.env 不會進版控）。
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"
