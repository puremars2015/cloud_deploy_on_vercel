{
  "name": "cloud-deploy-on-vercel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "示範在 Vercel 上部署：靜態首頁 + Serverless API + Neon Postgres",
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2"
  }
}
